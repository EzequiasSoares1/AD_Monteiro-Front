import React from "react";


const MaxWidth = (props) => {
  
    const props = {
        sm: 340,
        lg:  880,
        xl: 1920,
    }

    return(
        <div className={props.xl} >

        </div>
    );
    
};

export default MaxWidth;